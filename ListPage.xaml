<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ArdeleanDariaLab7"
    x:Class="ArdeleanDariaLab7.ListPage"
    Title="ListPage">

    <StackLayout Margin="20">

        <!-- Editor cu ValidationBehaviour -->
        <Editor 
            Placeholder="Enter the description of the shopping list"
            Text="{Binding Description}"
            HeightRequest="50"
            BackgroundColor="White"
            TextColor="Black"
            PlaceholderColor="DarkGray">

            <Editor.Behaviors>
                <local:ValidationBehaviour />
            </Editor.Behaviors>

        </Editor>
        <Picker x:Name="ShopPicker" Title="Enter Shop Name"/>
        <!-- Grid cu Save și Delete -->
        <Grid Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button 
                Text="Save"
                Clicked="OnSaveButtonClicked" />

            <Button 
                Grid.Column="1"
                Text="Delete"
                Clicked="OnDeleteButtonClicked" />
        </Grid>

        <!-- ListView cu produsele selectate (PASUL 14) -->
        <ListView 
            x:Name="listViewProducts"
            Margin="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Detail="{Binding Description}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!--Buton cerinta lab 9 -->
        <Button 
            Text="Delete Item"
            MaximumWidthRequest="200"
            Clicked="OnDeleteItemClicked" />

        <!-- Buton pentru alegerea produselor -->
        <Button 
            Text="Add/Choose Item to buy"
            MaximumWidthRequest="300"
            Clicked="OnChooseButtonClicked" />

    </StackLayout>

</ContentPage>

